#include <stdio.h>

int ifif() {
	int a, b, c, d, e;

	if( c,d,a || (b||e) || (c && d) || (~b)) {
		e = a, b, c;
		return 0;
	}
	return 2;
}

int main() {

	int a = 0;
	int b = (a) ? 1 : 0;
	return 0;
}
